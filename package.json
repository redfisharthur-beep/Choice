{
  "name": "choice-app",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler@4 deploy --config cloudflare/wrangler.jsonc",
    "version": "npx wrangler@4 versions upload --config cloudflare/wrangler.jsonc"
  }
}
